name: Deploy to Render

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via Render Blueprint
        run: echo "Deploy automático gerenciado pelo Render via render.yaml"
